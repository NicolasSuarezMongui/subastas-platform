info:
  name: Realizar una puja
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/api/productos/1/pujas
  body:
    type: json
    data: |-
      {
        "usuarioId": 1,
        "monto": 100.00
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
